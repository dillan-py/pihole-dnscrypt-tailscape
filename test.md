# Secure Pi Seup using Pi-Hole, Pi-Vpn with DNSCrypt
###Setup initial configuration
##  Update Pi
```bash
sudo apt upgrade && sudo apt autoremove && sudo apt autopurge -y    # Update and clean up any unused programs

##dfdfdfdf
